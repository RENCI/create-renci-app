let initializeProject,inquirer;_114‍.x([["cli",()=>cli]]);_114‍.w("./initialize-project",[["initializeProject",["initializeProject"],function(v){initializeProject=v}]]);_114‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);


       function cli(args) {
  if (!args.length) {
    return
  }

  const projectName = args[0].trim()

  if (!projectName) {
    return
  }

  inquirer.prompt([
    {
      type: 'list',
      message: `Select the framework you\'ll be using for ${ projectName }:`,
      name: 'framework',
      choices: ['React'],
    }
  ]).then(answers => {
    const { framework } = answers
    console.log(`You've selected ${ framework } (whihc makes sense because it's the only option!)`)
    initializeProject(projectName)
  })

}
