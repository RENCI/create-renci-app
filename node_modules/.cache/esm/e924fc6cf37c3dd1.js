let initializeProject;_f37‍.x([["cli",()=>cli]]);_f37‍.w("./initialize-project",[["initializeProject",["initializeProject"],function(v){initializeProject=v}]]);

       function cli(args) {

  if (!args.length) {
    return
  }

  const projectName = args[0].trim()

  if (!projectName) {
    return
  }

  initializeProject(projectName)
}
